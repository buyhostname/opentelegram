extends layout

block content
  div(class="min-h-screen flex flex-col items-center justify-center p-4")
    div(class="max-w-md w-full bg-gray-800 rounded-lg shadow-xl p-8 text-center")
      div(class="text-6xl mb-6")= status === 404 ? '🔍' : '⚠️'
      
      h1(class="text-2xl font-bold mb-4")= title
      
      p(class="text-gray-400 mb-6")= message
      
      a(href="/" class="inline-block bg-blue-500 text-white py-2 px-6 rounded-lg font-semibold transition-all duration-200")
        | Go Home
